# cargo-deny configuration.
# Run with: cargo deny check

[graph]
targets = [
    { triple = "x86_64-unknown-linux-gnu" },
    { triple = "aarch64-unknown-linux-gnu" },
]

[advisories]
db-path = "~/.cargo/advisory-db"
db-urls = ["https://github.com/rustsec/advisory-db"]
yanked = "deny"
ignore = []

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "Apache-2.0 WITH LLVM-exception",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "BSL-1.0",
    "ISC",
    "Unicode-DFS-2016",
    "Unicode-3.0",
    "Zlib",
    "CC0-1.0",
]
confidence-threshold = 0.93

[bans]
multiple-versions = "warn"
wildcards = "deny"
# Internal workspace crates are referenced as `{ workspace = true }`, which
# resolves to a path dependency without a version. That is the canonical
# pattern for non-published workspace members; we exempt path deps from the
# wildcard ban so external `"*"` deps (which we don't want) are still caught.
allow-wildcard-paths = true
highlight = "all"
deny = [
    # No high-level KV engines allowed.
    { name = "rocksdb" },
    { name = "sled" },
    { name = "redb" },
]

[sources]
unknown-registry = "deny"
unknown-git = "deny"
allow-registry = ["https://github.com/rust-lang/crates.io-index"]
