//! `paddock-core` — the PaddockKV LSM-tree engine.
//!
//! This crate is the storage engine. Subsequent commits land submodules one
//! at a time per the roadmap in `docs/ARCHITECTURE.md`. This initial commit
//! is the workspace scaffolding only — no engine code yet.

#![cfg_attr(docsrs, feature(doc_cfg))]
